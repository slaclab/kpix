File: ftdi/Readme.txt

This directory contains header files and a static library used
by the SidLink object when operating in direct USB mode. These
files are downloaded directly from www.ftdi.com. 

This directory does not need to be included in the library search
path due to static linking.
